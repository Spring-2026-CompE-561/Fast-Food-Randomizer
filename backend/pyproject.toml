[project]
name = "fast-food-randomizer"
version = "0.1.0"
description = "Randomly picks a fast-food restaurant (optionally filtered by category, price, etc.)"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi[all]>=0.128.8",
    "pwdlib[argon2]>=0.2.1",
    "pyjwt>=2.12.1",
    "sqlalchemy>=2.0.46",
]
